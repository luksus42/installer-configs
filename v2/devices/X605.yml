name: "Lenovo Smart Tab M10 X605F/L"
codename: "X605"
formfactor: "tablet"
aliases: ["X605F", "X605L", "lenovo-x605", "tb-x605"]
doppelgangers: []
user_actions:
  confirm_model:
    title: "Confirm your model"
    description: "Please make sure, that your device is a Lenovo Smart Tab M10 X605L or X605F with at least 3GB RAM. For the 2GB/16GB models, the installer won't work!"
  confirm_os:
    title: "Confirm OS version"
    description: >
      Your device must be running Android 9 before installing Ubuntu Touch. This seems to be already the case for the most devices of this type. 
      But if you still need to flash the Android 9 stock rom, please see the install section in the link below:
    link: "https://gitlab.com/ubports/porting/community-ports/android9/lenovo-tab-m10-fhd/lenovo-x605#install-first-time"
  bootloader:
    title: "Reboot to Bootloader"
    description: >
      With the device powered off, press and hold the VOLUME DOWN and POWER buttons at the same time until the device turns on. 
      The bootloader mode is indicated by the red 'Fastboot Mode' text in the left down corner.
    image: "phone_power_down"
    button: true
  recovery:
    title: "Reboot to Recovery"
    description: >
      Booting to recovery is not a very reliable process on this device. So you need to turn off the device first:
      Unplug USB-cable and press and hold the POWER button until display turns off. If it is rebooting, repeat this until the device stays off!
      If it is off, press and hold the VOLUME UP, VOLUME DOWN and the POWER buttons at the same time until it boots.
      Then only release the POWER button and keep holding the VOLUME buttons until the UBports recovery appears.
    button: true
  lvm-migration:
    title: "LVM Migration"
    description: >
      On first install of the new recovery, we need to increase the available size for the rootfs.
        When recovery is booted, wait until "lvm-migration" is finished. Watch the log output in the bottom of the device.
        If there is no output at all or when it displays, that it is finished, push the "Continue"-Button in the installer.
    button: true
unlock:
  - "confirm_model"
  - "confirm_os"
handlers:
  bootloader_locked:
    actions:
      - fastboot:flashing_unlock:
operating_systems:
  - name: "Ubuntu Touch"
    compatible_installer: ">=0.9.4-beta"
    options:
      - var: "channel"
        name: "Channel"
        tooltip: "The release channel"
        link: "https://docs.ubports.com/en/latest/about/process/release-schedule.html"
        type: "select"
        remote_values:
          systemimage:channels:
      - var: "wipe"
        name: "Wipe Userdata"
        tooltip: "Wipe personal data, mandatory on first install!"
        type: "checkbox"
      - var: "bootstrap"
        name: "Bootstrap"
        tooltip: "Flash system partitions using fastboot"
        type: "checkbox"
        value: true
    prerequisites: []
    steps:
      - actions:
          # latest artifacts from https://gitlab.com/ubports/porting/community-ports/android9/lenovo-tab-m10-fhd/lenovo-x605
          - core:download:
              group: "firmware"
              files:
                - url: "https://gitlab.com/api/v4/projects/27989875/jobs/artifacts/focal/download?job=build"
                  name: "artifacts.zip"
          - core:unpack:
              group: "firmware"
              files:
                - archive: "artifacts.zip"
                  dir: "unpacked"
          - core:unpack:
              group: "firmware"
              files:
                - archive: "unpacked/out/device_X605.tar.xz"
                  dir: "unpacked"
          - core:unpack:
              group: "firmware"
              files:
                - archive: "unpacked/device_X605.tar"
                  dir: "unpacked"
        condition:
          var: "bootstrap"
          value: true
      - actions:
          - adb:reboot:
              to_state: "bootloader"
        fallback:
          - core:user_action:
              action: "bootloader"
      - actions:
          - fastboot:flash:
              partitions:
                - partition: "recovery"
                  file: "unpacked/partitions/recovery.img"
                  group: "firmware"
                  raw: true
                - partition: "boot"
                  file: "unpacked/partitions/boot.img"
                  group: "firmware"
                  raw: true
        condition:
          var: "bootstrap"
          value: true
      - actions:
          - fastboot:format:
              partition: "userdata"
              type: "ext4"
        condition:
          var: "wipe"
          value: true
      - actions:
          - fastboot:erase:
              partition: "userdata"
        condition:
          var: "wipe"
          value: true
      - actions:
          - adb:reboot:
              to_state: "recovery"
        fallback:
          - core:user_action:
              action: "recovery"
      - actions:
          - core:user_action:
              action: "lvm-migration"
        condition:
          var: "bootstrap"
          value: true
      - actions:
          - systemimage:install:
      - actions:
          - adb:reboot:
              to_state: "recovery"
        fallback:
          - core:user_action:
              action: "recovery"
    slideshow: []
